import { Platform, ScreenBounds } from '../types/recorder/common';
import { AppetizeApp } from '../types/app';
import { ActionMapper } from './action';
import { ElementMapper } from './element';
export declare class SessionEventMapper {
    platform?: Platform;
    screen?: ScreenBounds;
    app?: AppetizeApp;
    actionMapper: ActionMapper;
    elementMapper: ElementMapper;
    constructor({ platform, screen, app, }: {
        platform: Platform;
        screen: ScreenBounds;
        app?: AppetizeApp;
    });
    toInternal(type: string, value: any): {
        type: string;
        value: any;
    };
    toPublic(type: string, value: any): {
        type: string;
        value: any;
    } | null;
}
export declare class ClientEventMapper {
    toInternal(type: string, value: any): {
        type: string;
        value: any;
    };
    toPublic(type: string, value: any): {
        type: string;
        value: any;
    };
}
