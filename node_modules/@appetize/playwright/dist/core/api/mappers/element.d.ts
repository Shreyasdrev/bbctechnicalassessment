import { Platform, ScreenBounds } from '../types/recorder/common';
import * as InternalRecorderAPI from '../types/recorder/internal';
import * as PublicRecorderAPI from '../types/recorder/public';
export declare class ElementMapper {
    platform: Platform;
    screen: ScreenBounds;
    constructor({ platform, screen, }: {
        platform: Platform;
        screen: ScreenBounds;
    });
    private pixelToDip;
    private dipToPixel;
    toInternal<T extends PublicRecorderAPI.Element | PublicRecorderAPI.ElementSelector>(element: T): T extends PublicRecorderAPI.Element ? InternalRecorderAPI.Element : InternalRecorderAPI.ElementSelector;
    toPublic<T extends InternalRecorderAPI.Element | InternalRecorderAPI.ElementSelector>(element: T): T extends InternalRecorderAPI.Element ? PublicRecorderAPI.Element : PublicRecorderAPI.ElementSelector;
}
