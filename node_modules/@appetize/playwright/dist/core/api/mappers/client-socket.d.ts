import { EventEmitter } from '../../EventEmitter';
import { SocketProtocol } from '../types/socket';
export declare class ClientSocketMapper extends EventEmitter implements SocketProtocol {
    private _socket;
    private eventMapper;
    constructor({ socket }: {
        socket: SocketProtocol;
    });
    private mapEmit;
    private mapSend;
    send(event: string, data?: any): Promise<void>;
    disconnect(): Promise<void>;
}
