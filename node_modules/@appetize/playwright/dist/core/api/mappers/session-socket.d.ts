import { EventEmitter } from '../../EventEmitter';
import { Platform, ScreenBounds } from '../types/recorder/common';
import { AppetizeApp } from '../types/app';
import { SocketProtocol } from '../types/socket';
export declare class SessionSocketMapper extends EventEmitter implements SocketProtocol {
    platform: Platform;
    screen: ScreenBounds;
    app?: AppetizeApp;
    private _socket;
    constructor({ socket, platform, screen, app, }: {
        socket: SocketProtocol;
        platform: Platform;
        screen: ScreenBounds;
        app?: AppetizeApp;
    });
    send(event: string, data?: any): Promise<void>;
    disconnect(): Promise<void>;
    private handleEvent;
    private mapEmit;
    private mapSend;
}
