import { SessionSocketMapper } from '../api/mappers/session-socket';
import { AppetizeApp } from '../api/types/app';
import { DeviceInfo } from '../client';
import { Session, SessionConfig } from '../session';
import { MockSocket } from './mock-socket';
export declare class MockSession extends Session<any> {
    socket: Omit<SessionSocketMapper, 'socket'> & {
        _socket: MockSocket;
    };
    constructor({ config, device, app, }: {
        config: SessionConfig;
        device: DeviceInfo;
        app?: AppetizeApp;
    });
    waitUntilReady(): Promise<void>;
}
