import { ClientSocketMapper } from '../api/mappers/client-socket';
import { Client } from '../client';
import { MockSocket } from './mock-socket';
export declare class MockClient extends Client<any, any, any> {
    socket: Omit<ClientSocketMapper, 'socket'> & {
        _socket: MockSocket;
    };
    constructor();
}
