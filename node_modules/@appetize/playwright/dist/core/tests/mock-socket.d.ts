import { EventEmitter } from '../EventEmitter';
import { SocketProtocol } from '../api/types/socket';
export declare class MockSocket extends EventEmitter implements SocketProtocol {
    constructor();
    send(): Promise<void>;
    disconnect(): Promise<void>;
    emit(eventName: string | symbol, value?: any): boolean;
}
