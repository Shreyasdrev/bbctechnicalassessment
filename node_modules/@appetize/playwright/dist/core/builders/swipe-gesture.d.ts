import { SwipeMove } from '../api/types/recorder';
export interface SwipeGestureArgs {
    duration?: number;
    stepDuration?: number;
}
export declare class SwipeGesture {
    private moves;
    private duration?;
    private stepDuration;
    constructor({ duration, stepDuration }: SwipeGestureArgs);
    to(x: string, y: string): this;
    wait(duration: number): this;
    build(): SwipeMove[];
    up(distance?: string): this;
    down(distance?: string): this;
    left(distance?: string): this;
    right(distance?: string): this;
}
