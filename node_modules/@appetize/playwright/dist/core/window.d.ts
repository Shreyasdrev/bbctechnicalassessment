import { type EventEmitter } from './EventEmitter';
export interface AppetizeWindowProtocol extends EventEmitter {
    waitUntilReady: () => Promise<void>;
    postMessage<T = undefined>(message: any, waitForResponse?: boolean): Promise<T>;
}
