import { PlaywrightSession } from '../session';
import { TestInfo } from '@playwright/test';
export declare class Codegen {
    private session;
    private testInfo;
    private currentRecord;
    constructor({ testInfo, session, }: {
        testInfo: TestInfo;
        session: PlaywrightSession;
    });
    record(): Promise<void>;
}
