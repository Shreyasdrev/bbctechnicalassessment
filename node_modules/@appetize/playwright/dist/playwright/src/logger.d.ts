import { LogFn, Logger } from '../../core/logger';
export declare class PlaywrightLogger extends Logger {
    logHistory: Array<{
        method: 'log' | 'warn' | 'error' | 'debug';
        data: any[];
    }>;
    logLevel: 'verbose' | 'warnings-errors' | 'none';
    log: LogFn;
    warn: LogFn;
    error: LogFn;
    debug: LogFn;
    clearLogHistory: () => void;
    private createPlaywrightLogFn;
}
export declare const logger: PlaywrightLogger;
